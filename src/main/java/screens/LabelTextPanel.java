package screens;

import javax.swing.*;

public class LabelTextPanel extends JPanel {
    public LabelTextPanel(JPanel label, JTextField textField) {
        this.add(label);
        this.add(textField);
    }
}
